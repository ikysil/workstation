---

# System display
display: ":0.0"

resolvd_docker_listener: 172.17.0.1

# Shared directory to install Linuxbrew in
linuxbrew_prefix_shared: "/home/linuxbrew/.linuxbrew"

# Install using the installer script
linuxbrew_use_installer: true
